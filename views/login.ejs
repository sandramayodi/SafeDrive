<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>SafeDrive AI - Login</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="/log.css" />
</head>
<body>
  <div class="container">
    <div class="header">
      <div class="logo">SafeDrive AI</div>
      <div class="tagline">Intelligent Intoxication Detection System</div>
    </div>

    <% if (locals.message) { %>
        <div class="alert"><%= locals.message %></div>
    <% } %>

    <div class="form-container">
      <form action="/login" method="POST">
        <h2 class="form-title">Welcome Back</h2>
        <p class="form-subtitle">Log in to your SafeDrive account</p>

        <div class="form-group">
          <label for="email">Email Address</label>
          <input type="email" id="email" name="email" placeholder="you@example.com" required />
        </div>

        <div class="form-group">
          <label for="password">Password</label>
          <input type="password" id="password" name="password" placeholder="Enter your password" required />
        </div>

        <button type="submit" class="submit-btn">Sign In</button>

        <div class="toggle-section">
          <span class="toggle-text">Don't have an account?
            <a href="/register" class="toggle-link">Sign Up</a>
          </span>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
